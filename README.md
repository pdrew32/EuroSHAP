# EuroSHAP

The goal of this repo is to:

1) Explore the EuroSAT dataset included with pytorch
2) Fit machine learning models to a training subset using pytorch
3) Assess the quality of the models
4) Use `shap.DeepExplainer` to highlight pixels that are associated with the label to verify that the model is working as expected.
